services:
  shortlink:
    build: .
    ports:
      - "8000:8000"
    environment:
      BASE_URL: ${BASE_URL:-http://localhost:8000}
      STORAGE_BACKEND: ${STORAGE_BACKEND:-memory}
    restart: unless-stopped
